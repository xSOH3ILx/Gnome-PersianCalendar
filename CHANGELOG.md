# Changelog

## [1.2.0] - 2026-09-02

### Added
- ویرایش و حذف رویدادهای شخصی از داخل پاپ‌آپ (دکمه‌های «ویرایش» و «حذف» روی هر رویداد شخصی با نشانگر ✏️)
- یادآور رویداد (VALARM): بدون یادآور، ۱۰/۳۰ دقیقه، ۱ ساعت یا ۱ روز قبل
- نشانگر (خط آبی) زیر روزهایی که رویداد شخصی یا گوگل دارند در گرید ماه
- لایه‌ی مناسبت‌های افزودنی با JSON: `events/extra-events.json` همراه افزونه یا `~/.config/shamsi-calendar/extra-events.json` کاربر — بدون تغییر کد (راهنما: docs/EVENTS.md)
- گردش‌کار انتشار خودکار: با پوش هر تگ `v*`، بسته‌ی افزونه ساخته و در GitHub Release منتشر می‌شود

### Changed
- ناوبری کیبورد، جستجوی مناسبت‌ها و پیش‌نمایش زنده به v1.3.0 منتقل شد (docs/ROADMAP.md)

## [1.1.0] - 2026-09-02

### Added
- افزودن رویداد شخصی از داخل پاپ‌آپ (تمام‌روز یا ساعت‌دار) با ذخیره در Evolution Data Server — همگام‌سازی خودکار با GNOME Calendar و (از طریق GNOME Online Accounts) با Google Calendar
- انتخاب تقویم مقصد (شخصی، گوگل و …) در فرم افزودن رویداد
- ماژول جدید `EDS.js` برای ارتباط مستقیم با EDS از طریق D-Bus

### Fixed
- راست‌چین کامل ردیف رویدادها: نشانگر (■/آیکون) حالا سمت راست متن قرار می‌گیرد، مستقل از زبان سیستم
- راست‌چین شدن نوار تب‌ها (تب اول سمت راست) و ردیف‌های نتایج تبدیل تاریخ (دکمهٔ کپی سمت چپ)

## [1.0.0] - 2026-09-01

### Added
- دکمه‌ی «امروز» در هدر تقویم برای برگشت سریع به تاریخ جاری
- کلید روشن/خاموش اعلان شروع در تنظیمات (کلید schema قبلاً مخفی بود)
- کلیک‌پذیر شدن ردیف مخزن سورس‌کد در صفحه‌ی درباره
- راست‌چین شدن متن رویدادها و نتایج تبدیل تاریخ در پنل چپ
- نقشه‌ی راه (docs/ROADMAP.md) — رویداد شخصی با سینک گنوم/گوگل در v1.1.0

### Fixed
- رفع خطای نمایش نسخه در صفحه‌ی درباره (عدم انتقال metadata به کلاس تنظیمات)
- رفع `ReferenceError` های موتور تبدیل تاریخ (`Tarikh.js`)
- رفع رگرسیون `googleCalendarSync` در `Events.js`
- تشخیص درست رویدادهای تمام‌روز (local + UTC midnight) و پایان بازه‌ی exclusive
- رفع بهم‌ریختگی BiDi عنوان رویدادهای گوگل با Unicode Isolate (LRI/PDI)
- رفع خرابی encoding در داده‌ی سال ۱۳۸۳ فایل `tahvil.js`
- اصلاح چرخه‌ی حیات افزونه: پاکسازی کامل در `disable()`، تایمر نیمه‌شب به‌جای polling ۶۰ ثانیه‌ای
- نمایش نسخه در صفحه‌ی «درباره» از `metadata.json` (قبلاً هاردکد «45»)

### Changed
- بازسازی ساختار ریپو به چیدمان استاندارد (`src/`, `tests/`, `po/`, `docs/`)
- افزودن زیرساخت i18n با gettext (۶۴ رشته‌ی قابل ترجمه، زبان مبدأ فارسی)
- افزودن تست‌های خودکار موتور تقویم + ESLint + GitHub Actions CI
- استانداردسازی `metadata.json` (version-name، gettext-domain، url جدید)
- گذار به نسخه‌بندی مستقل SemVer (جدا از شماره‌نسخه‌های پروژه‌ی مادر)
